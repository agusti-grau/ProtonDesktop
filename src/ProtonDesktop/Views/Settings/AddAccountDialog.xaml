<Window x:Class="ProtonDesktop.Views.Settings.AddAccountDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:conv="clr-namespace:ProtonDesktop.Converters"
        mc:Ignorable="d"
        Title="Add Account"
        Width="480"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <conv:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10">
            Enter your ProtonMail Bridge credentials.<LineBreak/>
            <Run Foreground="#666" FontSize="12">Get the password from Bridge → Mailbox details (not your Proton password).</Run>
        </TextBlock>

        <!-- Email / Display Name -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Email:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Display Name:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Password:" VerticalAlignment="Center"/>
            <PasswordBox Grid.Row="4" Grid.Column="1" x:Name="PasswordBox" Padding="5" PasswordChanged="PasswordBox_PasswordChanged"/>
        </Grid>

        <!-- Server Settings -->
        <Expander Grid.Row="2" Header="Server Settings (defaults match ProtonMail Bridge)" IsExpanded="True" Margin="0,0,0,10">
            <Grid Margin="10,5,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="85"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="IMAP:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ImapHost, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Port:" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding ImapPort, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="SMTP:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SmtpHost, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                <TextBlock Grid.Row="2" Grid.Column="2" Text="Port:" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding SmtpPort, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="CalDAV:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding CaldavHost, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                <TextBlock Grid.Row="4" Grid.Column="2" Text="Port:" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <TextBox Grid.Row="4" Grid.Column="3" Text="{Binding CaldavPort, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
            </Grid>
        </Expander>

        <!-- Status -->
        <DockPanel Grid.Row="3" Margin="0,0,0,10">
            <ProgressBar DockPanel.Dock="Right" Width="60" Height="4" IsIndeterminate="True"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding StatusMessage}" Foreground="#666" TextWrapping="Wrap"/>
        </DockPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Test Connection" Command="{Binding TestConnectionCommand}" Padding="12,6" Margin="0,0,5,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Padding="12,6" Margin="0,0,5,0"/>
            <Button Content="Save" Command="{Binding SaveCommand}" Padding="12,6" Background="#0078D4" Foreground="White"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolToVisibility}}"/>
        </StackPanel>
    </Grid>
</Window>
