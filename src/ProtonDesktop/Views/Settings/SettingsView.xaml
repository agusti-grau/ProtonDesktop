<Window x:Class="ProtonDesktop.Views.Settings.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Settings" 
        Height="600" 
        Width="800"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Account List -->
        <Border Grid.Column="0" BorderBrush="#E1E1E1" BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Accounts" FontWeight="Bold" Padding="10,10,10,5"/>

                <ListBox Grid.Row="1" ItemsSource="{Binding Accounts}" 
                         SelectedItem="{Binding SelectedAccount}"
                         Margin="5">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Email}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding DisplayName}" FontSize="11" Foreground="#666666"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5">
                    <Button Content="Add" Command="{Binding AddAccountCommand}" Padding="10,5" Margin="0,0,5,0"/>
                    <Button Content="Remove" Command="{Binding RemoveAccountCommand}" Padding="10,5" Margin="0,0,5,0"/>
                    <Button Content="Set Default" Command="{Binding SetDefaultAccountCommand}" Padding="10,5"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Right Panel: Settings -->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- ProtonMail Bridge Settings -->
                <TextBlock Text="ProtonMail Bridge Settings" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="IMAP Host:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ImapHost, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                    <TextBlock Grid.Row="0" Grid.Column="3" Text="IMAP Port:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="4" Text="{Binding ImapPort, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="SMTP Host:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SmtpHost, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                    <TextBlock Grid.Row="2" Grid.Column="3" Text="SMTP Port:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="4" Text="{Binding SmtpPort, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="CalDAV Host:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding CalDavHost, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                    <TextBlock Grid.Row="4" Grid.Column="3" Text="CalDAV Port:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="4" Grid.Column="4" Text="{Binding CalDavPort, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                </Grid>

                <!-- Sync Settings -->
                <TextBlock Text="Sync Settings" FontWeight="Bold" FontSize="14" Margin="0,20,0,10"/>
                
                <StackPanel Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Sync Interval (minutes):" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding SyncIntervalMinutes, UpdateSourceTrigger=PropertyChanged}" Padding="5" Width="100" HorizontalAlignment="Left"/>
                    </Grid>
                </StackPanel>

                <!-- Notification Settings -->
                <TextBlock Text="Notification Settings" FontWeight="Bold" FontSize="14" Margin="0,20,0,10"/>
                
                <StackPanel Margin="0,0,0,15">
                    <CheckBox Content="Show notifications for new emails" IsChecked="{Binding ShowNotifications}" Margin="0,5"/>
                    <CheckBox Content="Start minimized" IsChecked="{Binding StartMinimized}" Margin="0,5"/>
                    <CheckBox Content="Minimize to system tray" IsChecked="{Binding MinimizeToTray}" Margin="0,5"/>
                </StackPanel>

                <!-- Save Button -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#666666"/>
                    <Button Content="Save" Command="{Binding SaveCommand}" Padding="20,8" Background="#0078D4" Foreground="White"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
